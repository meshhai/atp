2026-06-11: Combined the hardening, health/readiness, and operational docs plans into one PR plan at user request. Run this single feature with Ralph instead of the previous three separate feature slugs.
2026-06-11 10:40 JST: Worked ISSUE-001 runtime coverage baseline. Selected it because it is the unblocked prerequisite for the dispatcher/runtime hardening chain. Fetched missing deps with `mix deps.get`, then ran `mix test`: 301 tests, 0 failures. Ran `mix test --cover`: 301 tests, 0 failures, generated HTML coverage, but the coverage gate failed at 95.19% against the configured 100.00% threshold.
Coverage baseline target list for later issues:
- `Atp.Transport.WebhookDispatcher.AttemptWorker` 77.27%: cover `claim/1` nil/non-claim paths, catch-based delivery failure handling, and sanitized task-exit recording fallback when recording itself raises or throws.
- `Atp.Transport.WebhookDispatcher` 78.05%: cover stale worker result/task-exit messages, `wakeup(nil)`, wakeup to missing/dead dispatchers, invalid option fallback defaults, dispatch-on-start scheduling, non-worker/failed attempt supervisor discovery, claim error pending reset, `DynamicSupervisor.start_child/2` returning `{:ok, pid, info}`, shutdown wait paths for normal/shutdown/crash exits, nil claim telemetry guards, and remaining coarse result/error metadata branches.
- `Atp.Transport.SessionIntake` 80.00%: cover prepared intake with a non-webhook commit value so no dispatcher wakeup occurs.
- `Atp.Transport.Runtime.SessionServer` 83.93%: cover refresh errors, unknown dispatch tickets, wrong release tokens, stale monitor messages, pending expiry not-due/error/non-pending no-op paths, and stale/invalid queued webhook dispatch tickets.
- `Atp.Transport.Runtime` 85.71% and `Atp.Transport.Runtime.PendingSessionRehydrator` 86.36%: cover session start/summary failure normalization, warm-start failure logging/catch paths, reject/message-expired process stop paths, safe stop exits, pending rehydrator default start, catch handling, and already-started session rehydration.
- `Atp.CLI` 85.14% remains a secondary target only for production-relevant CLI behavior; prioritize runtime/dispatcher/session edges first per the PRD.
